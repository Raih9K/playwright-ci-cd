import { test, expect } from '@playwright/test';
import testData from '../test-data/testData.json';

test.describe('Tests Suite name', () => {

    test('test name', async ({ page }) => {
        // Test steps
        // 1. Navigate to the registration page
        // 2. Enter valid registration details
        // 3. Click the register button
        // 4. Validate the successful registration message
        // 5. Validate the user is redirected to the login page
        // 6. Validate the user can log in with the registered credentials
        // 7. Validate the user is redirected to the dashboard page
        // 8. Log out and validate the user is redirected to the login pag
    }); 
});
    